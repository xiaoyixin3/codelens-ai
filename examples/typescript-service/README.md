# Example repository policy

This directory demonstrates a minimal TypeScript service policy. Copy the two policy files into a test repository, open a pull request, and inspect the CodeLens Check summary before enabling blocking mode.
